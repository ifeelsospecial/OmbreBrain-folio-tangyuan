# ============================================================
# Ombre-Brain 环境变量示例
# 复制为 .env 文件后填入你的实际值
# 云部署 (Render/Railway/etc) 在 dashboard 里配置同样的变量
# ============================================================

# ─── 必填: LLM 模型配置 ───────────────────────────────────────

# LLM API key (DeepSeek / Gemini / Claude / OpenAI 等任意 OpenAI 兼容 API)
OMBRE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# LLM API base URL
# DeepSeek:    https://api.deepseek.com/v1
# Gemini:      https://generativelanguage.googleapis.com/v1beta/openai/
# Claude:      https://api.anthropic.com/v1/
# OpenAI:      https://api.openai.com/v1
# 硅基流动:    https://api.siliconflow.cn/v1
# DashScope:   https://dashscope.aliyuncs.com/compatible-mode/v1
OMBRE_BASE_URL=https://api.deepseek.com/v1

# 模型名称 (跟你选的 base URL 对应)
# 例: deepseek-chat / gemini-2.5-flash / claude-haiku-4-5 / gpt-4o-mini
OMBRE_MODEL=deepseek-chat


# ─── 可选: Embedding 模型 (独立配置, 给"全库相似" / 主动浮现等功能用) ───
# 不配的话, 跟 OMBRE_API_KEY 一样的 key, 但 base URL 用下面的
# 强烈推荐 gemini-embedding-001 (3072 维, 与默认一致; 换模型在 config.yaml 的 embedding.model 改)

OMBRE_EMBED_API_KEY=
OMBRE_EMBED_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
# 云端默认 openai_compat；本地 Ollama 设 ollama（本地模式永不转发云端 Key）
OMBRE_EMBED_FORMAT=openai_compat
# OMBRE_OLLAMA_URL=http://127.0.0.1:11434/v1

# ─── 可选: API 请求超时 (秒) ───
# 自托管服务器连云端 API 慢时调大 (默认: 脱水/打标 60, embedding 30)
# OMBRE_COMPRESS_TIMEOUT_SECONDS=60
# OMBRE_EMBED_TIMEOUT_SECONDS=30

# ─── 可选: Docker 部署的宿主机挂载目录 (docker-compose.yml 用) ───
# 把记忆数据存到你指定的宿主机目录(如 Obsidian Vault); 不设则用项目内 ./buckets
# OMBRE_HOST_VAULT_DIR=/path/to/your/Obsidian Vault/Ombre Brain


# ─── 必填: 数据存储路径 ──────────────────────────────────────

# 记忆桶存储目录 (绝对路径推荐, 云部署用持久盘挂载点)
# 本地开发:     ./buckets
# Render:       /opt/render/project/src/buckets
# Railway:      /app/data/buckets (需挂载 volume)
OMBRE_BUCKETS_DIR=./buckets

# 附件默认持久化到 OMBRE_BUCKETS_DIR/_media；单项默认上限 25 MiB
# OMBRE_MEDIA_DIR=/absolute/path/to/persistent/media
# OMBRE_MEDIA_MAX_BYTES=26214400

# Obsidian/Git/手工编辑后的缓存检查间隔（秒）；0 = 每次读取都检查
# OMBRE_EXTERNAL_CHANGE_POLL_SECONDS=1


# ─── 必填: 传输模式 ─────────────────────────────────────────

# stdio:           本地用 (Claude Desktop / Cursor / VSCode 等 MCP 客户端)
# streamable-http: 云部署 / 远程访问 (HTTP API + 网页前端)
OMBRE_TRANSPORT=streamable-http


# ─── 🔴 公网必填: 管理员 token (全局鉴权) ──────────────────────
# 设一个强随机值 (例: openssl rand -hex 32 / python -c "import secrets;print(secrets.token_hex(32))")。
# OB 现在全局要门: 除静态页 + /health 外, 所有 /api/* + /mcp 都必须带
# X-Admin-Token header (值 = 这个 token), 否则 401。
# ⚠ 公网 transport (streamable-http/sse) 没设这个会【拒绝启动】——
#   没有它任何拿到 URL 的人都能读/删你的全部记忆。
# (确知在私网 / 反代后已有鉴权才想跳过的话, 设 OMBRE_ALLOW_NO_AUTH=1 显式豁免。)
OMBRE_ADMIN_TOKEN=

# 显式豁免全局鉴权: 仅限私网 / 反代已自行鉴权的场景。公网设这个 = 把记忆裸奔给所有人。
# OMBRE_ALLOW_NO_AUTH=1

# ─── 可选: claude.ai 网页版连接器专用 URL 密钥 (Option ①) ──────
# claude.ai 网页版自定义连接器只有 URL 字段、配不了自定义 header → 连不了上面的
# header 鉴权。设这个独立密钥 (强随机、URL 安全 hex, 别跟 OMBRE_ADMIN_TOKEN 同值),
# 连接器 URL 二选一 (两种都支持):
#   https://你的域名/<这个值>/mcp      ← 推荐: 密钥在路径里, 每个请求都带, 最稳
#   https://你的域名/mcp?key=<这个值>   ← 备选: 密钥在 query
# 即可放行 /mcp。它【只开 /mcp】(读写删记忆), 打不开 /api/* (那些仍只认 header token);
# 权限更小所以跟主 token 分开、可单独轮换。URL 里带密钥比 header 弱 (可能被截图/历史看到),
# 默认留空 = 关闭这条口子, 纯 header 模式不变。详见 DEPLOY.md 的 Option ①。
OMBRE_MCP_URL_KEY=

# 支持 OAuth 的 MCP 客户端可改为 oauth；管理 API 仍使用 OMBRE_ADMIN_TOKEN。
OMBRE_MCP_AUTH_MODE=admin
# OAuth 必须知道稳定的公开 HTTPS 地址（不要带末尾 /）。
# OMBRE_PUBLIC_BASE_URL=https://memory.example.com

# ─── 可选: CORS 允许的浏览器跨源 origin (逗号分隔) ─────────────
# 默认空 = 仅同源 (dashboard 由本服务同源伺服, 一般够用)。
# 只有你从【另一个域名的网页】用浏览器 JS 直接打 /api 才需要填,
# 例: https://my-frontend.example.com,https://other.example.com
OMBRE_ALLOWED_ORIGINS=

# 只信任这些“最后一跳”反代提供的 X-Forwarded-Proto/Host；默认仅本机反代。
OMBRE_TRUSTED_PROXY_CIDRS=127.0.0.0/8,::1/128

# ─── 可选: Tunnel / hooks / 自更新 ───────────────────────────
# cloudflared 不会自动下载；安装后可在「部署连接」页启停。
# OMBRE_CLOUDFLARED_PATH=/absolute/path/to/cloudflared
# OMBRE_TUNNEL_AUTOSTART=0
# OMBRE_TUNNEL_TARGET=http://127.0.0.1:8000

# dream-hook 默认保留兼容；设 0 后端点返回 404。
OMBRE_ENABLE_DREAM_HOOK=1

# 自更新默认检查你自己的 folio 仓库；下载/应用和进程重启均需显式开启。
OMBRE_UPDATE_REPO=ceshihaox-dotcom/OmbreBrain-folio
OMBRE_UPDATE_REF=main
OMBRE_ENABLE_SELF_UPDATE=0
OMBRE_ENABLE_RESTART=0

# 多实例启动器使用；单实例无需配置。
# OMBRE_PORT=8000
# OMBRE_OWNER_NAME=
# OMBRE_OWNER_COUNT=1

# 脱水时的人物视角称谓，避免模型翻转主语。
HUMAN_NAME=用户
AI_NAME=AI


# ─── 可选: 自动备份 (强烈建议云部署用户配) ────────────────
# 配上 GitHub Actions 每天自动推 buckets/ 到你的私人 git 仓库
# 持久盘事故确有先例(曾丢约一周记忆), 这套是长期解药

# 私人 GitHub 仓库 (建一个空的, 设成 private)
# 例: https://github.com/yourname/my-ombre-backup   ← 必须带 https://
OMBRE_BACKUP_REPO=

# GitHub Personal Access Token (https://github.com/settings/tokens)
# 权限只需 repo (private 仓库写入)
OMBRE_BACKUP_TOKEN=

# 备份 commit 用的用户名 + email (可选)
OMBRE_BACKUP_USER=ombre-bot
